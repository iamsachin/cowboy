---
phase: 13-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/utils/tool-icons.ts
  - packages/frontend/src/utils/model-labels.ts
  - packages/frontend/src/app.css
  - packages/frontend/src/components/ToolCallRow.vue
  - packages/frontend/src/components/AssistantGroupCard.vue
  - packages/frontend/src/components/TurnCard.vue
  - packages/frontend/tests/utils/tool-icons.test.ts
  - packages/frontend/tests/utils/model-labels.test.ts
autonomous: true
requirements: [META-03, META-04]

must_haves:
  truths:
    - "Each tool call row displays a type-specific colored icon (not a generic Wrench for all)"
    - "Model name badges show short friendly labels instead of raw model ID strings"
    - "Model name badges have visually distinct colored backgrounds per model family"
    - "Unknown tool names fall back to Wrench icon with info color"
    - "Unknown model strings fall back to raw string with ghost badge styling"
  artifacts:
    - path: "packages/frontend/src/utils/tool-icons.ts"
      provides: "Tool name to icon component + color class mapping"
      exports: ["getToolIcon", "ToolIconInfo"]
    - path: "packages/frontend/src/utils/model-labels.ts"
      provides: "Model string to short label + badge CSS class mapping"
      exports: ["getModelBadge", "ModelBadgeInfo"]
    - path: "packages/frontend/src/app.css"
      provides: "Custom badge-model-* CSS classes with oklch colors"
      contains: "badge-model-opus"
    - path: "packages/frontend/tests/utils/tool-icons.test.ts"
      provides: "Unit tests for tool icon mapping"
    - path: "packages/frontend/tests/utils/model-labels.test.ts"
      provides: "Unit tests for model label mapping"
  key_links:
    - from: "packages/frontend/src/components/ToolCallRow.vue"
      to: "packages/frontend/src/utils/tool-icons.ts"
      via: "getToolIcon(props.toolCall.name) computed property"
      pattern: "getToolIcon"
    - from: "packages/frontend/src/components/AssistantGroupCard.vue"
      to: "packages/frontend/src/utils/model-labels.ts"
      via: "getModelBadge(props.group.model) computed property"
      pattern: "getModelBadge"
    - from: "packages/frontend/src/components/TurnCard.vue"
      to: "packages/frontend/src/utils/model-labels.ts"
      via: "getModelBadge(props.turn.message.model) computed property"
      pattern: "getModelBadge"
---

<objective>
Add visual differentiation to tool call rows (type-specific colored icons) and model name badges (color-coded with short labels) for quick scanning of conversation details.

Purpose: Users currently see a generic Wrench icon for all tool calls and raw model ID strings in ghost badges. This phase makes both visually distinct so users can scan conversation content at a glance.
Output: Two utility modules (tool-icons.ts, model-labels.ts), custom CSS badge classes, and updated ToolCallRow/AssistantGroupCard/TurnCard components.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-visual-polish/13-RESEARCH.md

@packages/frontend/src/components/ToolCallRow.vue
@packages/frontend/src/components/AssistantGroupCard.vue
@packages/frontend/src/components/TurnCard.vue
@packages/frontend/src/app.css

<interfaces>
<!-- Existing types and patterns the executor needs -->

From packages/frontend/src/components/ToolCallRow.vue:
```typescript
// Props - toolCall.name is the key for icon lookup
const props = defineProps<{
  toolCall: ToolCallRow;
}>();
// Currently: import { Wrench, Copy, Check } from 'lucide-vue-next';
// Line 5: <Wrench class="w-3.5 h-3.5 text-info shrink-0" />
```

From packages/frontend/src/components/AssistantGroupCard.vue:
```typescript
const props = defineProps<{
  group: AssistantGroup;
  expanded: boolean;
  tokenUsageByMessage?: Record<string, MessageTokenUsage>;
}>();
// Line 14-16: badge badge-sm badge-ghost showing group.model raw string
```

From packages/frontend/src/components/TurnCard.vue:
```typescript
const props = defineProps<{
  turn: AssistantTurn;
  expanded: boolean;
  nextTurn?: Turn;
}>();
// Line 14-17: badge badge-sm badge-ghost showing turn.message.model raw string
```

From packages/frontend/src/app.css (existing badge pattern):
```css
/* Claude Code badge — Anthropic orange */
.badge-claude {
  background-color: oklch(0.65 0.17 55);
  color: oklch(0.98 0 0);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tool-icons and model-labels utility modules with tests and CSS</name>
  <files>
    packages/frontend/src/utils/tool-icons.ts
    packages/frontend/src/utils/model-labels.ts
    packages/frontend/src/app.css
    packages/frontend/tests/utils/tool-icons.test.ts
    packages/frontend/tests/utils/model-labels.test.ts
  </files>
  <behavior>
    tool-icons.test.ts:
    - getToolIcon('Read') returns { icon: FileText, colorClass: 'text-sky-400' }
    - getToolIcon('Bash') returns { icon: Terminal, colorClass: 'text-emerald-400' }
    - getToolIcon('Edit') returns { icon: Pencil, colorClass: 'text-amber-400' }
    - getToolIcon('Write') returns { icon: FilePlus, colorClass: 'text-teal-400' }
    - getToolIcon('Grep') returns { icon: Search, colorClass: 'text-violet-400' }
    - getToolIcon('Glob') returns { icon: FolderSearch, colorClass: 'text-rose-400' }
    - getToolIcon('Agent') returns { icon: Bot, colorClass: 'text-fuchsia-400' }
    - getToolIcon('WebSearch') returns { icon: Globe, colorClass: 'text-cyan-400' }
    - getToolIcon('UnknownTool') returns { icon: Wrench, colorClass: 'text-info' } (fallback)
    - getToolIcon('TodoWrite') returns fallback (unknown tool)

    model-labels.test.ts:
    - getModelBadge('claude-3-5-sonnet-20241022') returns { label: 'Sonnet', cssClass: 'badge-model-sonnet' }
    - getModelBadge('claude-3-opus-20240229') returns { label: 'Opus', cssClass: 'badge-model-opus' }
    - getModelBadge('claude-3-haiku-20240307') returns { label: 'Haiku', cssClass: 'badge-model-haiku' }
    - getModelBadge('gpt-4o-2024-08-06') returns { label: 'GPT-4o', cssClass: 'badge-model-gpt4o' } (matched before gpt-4)
    - getModelBadge('gpt-4-turbo') returns { label: 'GPT-4', cssClass: 'badge-model-gpt4' }
    - getModelBadge(null) returns { label: '', cssClass: 'badge-ghost' } (fallback)
    - getModelBadge('unknown-model-v2') returns { label: 'unknown-model-v2', cssClass: 'badge-ghost' }
    - getModelBadge('gemini-1.5-pro') returns { label: 'Gemini', cssClass: 'badge-model-gemini' }
  </behavior>
  <action>
    1. Write tests FIRST (RED phase):
       - Create `packages/frontend/tests/utils/tool-icons.test.ts` with tests for all 8 known tools plus fallback
       - Create `packages/frontend/tests/utils/model-labels.test.ts` with tests for model matching, order sensitivity (gpt-4o before gpt-4), null handling, and unknown fallback
       - Run tests -- they MUST fail

    2. Implement utilities (GREEN phase):
       - Create `packages/frontend/src/utils/tool-icons.ts`:
         - Export interface ToolIconInfo { icon: Component; colorClass: string }
         - TOOL_ICONS record mapping: Read->FileText/sky-400, Bash->Terminal/emerald-400, Edit->Pencil/amber-400, Write->FilePlus/teal-400, Grep->Search/violet-400, Glob->FolderSearch/rose-400, Agent->Bot/fuchsia-400, WebSearch->Globe/cyan-400
         - FALLBACK: Wrench/text-info
         - Export function getToolIcon(toolName: string): ToolIconInfo
         - Import icons from lucide-vue-next: FileText, Terminal, Pencil, FilePlus, Search, FolderSearch, Bot, Globe, Wrench

       - Create `packages/frontend/src/utils/model-labels.ts`:
         - Export interface ModelBadgeInfo { label: string; cssClass: string }
         - MODEL_MATCHERS array ordered most-specific-first: opus, sonnet, haiku, gpt-4o, gpt-4, gpt-3, o1, o3, gemini, deepseek
         - CRITICAL: gpt-4o MUST come before gpt-4 in the array (substring matching order matters)
         - FALLBACK for null: { label: '', cssClass: 'badge-ghost' }
         - FALLBACK for unknown: { label: rawString, cssClass: 'badge-ghost' }
         - Export function getModelBadge(modelString: string | null): ModelBadgeInfo

    3. Add badge CSS classes to `packages/frontend/src/app.css`:
       - Add after existing .badge-claude block
       - Use soft-tint style: oklch(0.35 chroma hue) for background, oklch(0.80 chroma hue) for text
       - Classes: badge-model-opus (hue 280 purple), badge-model-sonnet (hue 320 pink), badge-model-haiku (hue 160 teal), badge-model-gpt4o (hue 145 green), badge-model-gpt4 (hue 145 green), badge-model-gpt35 (hue 200 cyan), badge-model-o1 (hue 80 yellow), badge-model-o3 (hue 55 orange), badge-model-gemini (hue 250 blue), badge-model-deepseek (hue 220 light blue)

    4. Run tests -- they MUST pass
  </action>
  <verify>
    <automated>cd packages/frontend && npx vitest run tests/utils/tool-icons.test.ts tests/utils/model-labels.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - getToolIcon returns correct icon+color for all 8 tool types and Wrench fallback for unknowns
    - getModelBadge returns correct label+class for known models, handles null and unknown gracefully
    - gpt-4o matches before gpt-4 (order test passes)
    - Badge CSS classes exist in app.css with oklch soft-tint colors
    - All tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ToolCallRow, AssistantGroupCard, and TurnCard components</name>
  <files>
    packages/frontend/src/components/ToolCallRow.vue
    packages/frontend/src/components/AssistantGroupCard.vue
    packages/frontend/src/components/TurnCard.vue
  </files>
  <action>
    1. Update ToolCallRow.vue:
       - Replace `import { Wrench, Copy, Check } from 'lucide-vue-next'` with `import { Copy, Check } from 'lucide-vue-next'` and `import { getToolIcon } from '../utils/tool-icons'`
       - Add computed: `const toolIcon = computed(() => getToolIcon(props.toolCall.name))`
       - Replace line 5 `<Wrench class="w-3.5 h-3.5 text-info shrink-0" />` with `<component :is="toolIcon.icon" class="w-3.5 h-3.5 shrink-0" :class="toolIcon.colorClass" />`
       - Keep the tool name text as neutral `base-content` (no color on text, per user decision: icon only colored)

    2. Update AssistantGroupCard.vue:
       - Add import: `import { getModelBadge } from '../utils/model-labels'`
       - Add computed: `const modelBadge = computed(() => getModelBadge(props.group.model))`
       - Replace lines 13-16 (the model badge span):
         Before: `<span v-if="group.model" class="badge badge-sm badge-ghost">{{ group.model }}</span>`
         After: `<span v-if="group.model" class="badge badge-sm" :class="modelBadge.cssClass">{{ modelBadge.label }}</span>`

    3. Update TurnCard.vue:
       - Add import: `import { getModelBadge } from '../utils/model-labels'`
       - Add computed: `const modelBadge = computed(() => getModelBadge(props.turn.message.model))`
       - Replace lines 14-17 (the model badge span):
         Before: `<span v-if="turn.message.model" class="badge badge-sm badge-ghost">{{ turn.message.model }}</span>`
         After: `<span v-if="turn.message.model" class="badge badge-sm" :class="modelBadge.cssClass">{{ modelBadge.label }}</span>`

    4. Verify the build compiles (catches icon import typos)
  </action>
  <verify>
    <automated>cd packages/frontend && npx vue-tsc --noEmit 2>&1 | head -20 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ToolCallRow renders dynamic icon per tool type with type-specific color, tool name text stays neutral
    - AssistantGroupCard shows short model label in colored badge (not raw model ID in ghost badge)
    - TurnCard shows short model label in colored badge (consistent with AssistantGroupCard)
    - Build succeeds with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
1. All unit tests pass: `cd packages/frontend && npx vitest run --reporter=verbose`
2. Build succeeds: `cd packages/frontend && npx vite build`
3. TypeScript compiles: `cd packages/frontend && npx vue-tsc --noEmit`
</verification>

<success_criteria>
- Tool call rows display type-specific colored icons (Read=FileText/sky, Bash=Terminal/emerald, etc.)
- Unknown tools show Wrench with info color (graceful fallback)
- Model badges show short labels ("Sonnet", "Opus", "Haiku") not raw IDs
- Model badges have distinct colored backgrounds per model family
- Both AssistantGroupCard and TurnCard show consistent model badge styling
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-visual-polish/13-01-SUMMARY.md`
</output>
