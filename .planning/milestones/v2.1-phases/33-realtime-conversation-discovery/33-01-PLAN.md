---
phase: 33-realtime-conversation-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/composables/useConversations.ts
  - packages/frontend/src/composables/useConversationBrowser.ts
  - packages/frontend/src/composables/useAnalytics.ts
  - packages/frontend/tests/composables/useConversations.test.ts
  - packages/frontend/tests/composables/useConversationBrowser.test.ts
autonomous: true
requirements:
  - PUSH-04
  - PUSH-05

must_haves:
  truths:
    - "useConversations subscribes to conversation:created, conversation:changed, and system:full-refresh WebSocket events"
    - "WebSocket-triggered refetches in both list composables are debounced at 500ms"
    - "WebSocket-triggered refetches do NOT reset page to 1"
    - "useAnalytics refetches are debounced at 500ms to prevent API flood"
    - "Both list composables expose newIds set for row highlight tracking"
    - "Both list composables use separate loading/refreshing refs so WS refetches don't show full spinner"
  artifacts:
    - path: "packages/frontend/src/composables/useConversations.ts"
      provides: "WebSocket subscription, debounced refetch, newIds tracking, separate loading/refreshing"
      contains: "on('conversation:created'"
    - path: "packages/frontend/src/composables/useConversationBrowser.ts"
      provides: "Debounced WS refetch replacing naive refetch, newIds tracking, separate loading/refreshing"
      contains: "debouncedWsRefetch"
    - path: "packages/frontend/src/composables/useAnalytics.ts"
      provides: "Debounced WS refetch replacing naive refetch"
      contains: "debouncedWsRefetch"
    - path: "packages/frontend/tests/composables/useConversations.test.ts"
      provides: "Unit tests for PUSH-05 behaviors"
    - path: "packages/frontend/tests/composables/useConversationBrowser.test.ts"
      provides: "Unit tests for PUSH-04 behaviors"
  key_links:
    - from: "packages/frontend/src/composables/useConversations.ts"
      to: "useWebSocket"
      via: "on() subscription"
      pattern: "on\\('conversation:created'"
    - from: "packages/frontend/src/composables/useConversationBrowser.ts"
      to: "useWebSocket"
      via: "debounced on() subscription"
      pattern: "debouncedWsRefetch"
---

<objective>
Add WebSocket-driven debounced refetch to all three list/analytics composables, with new-row tracking for UI highlight.

Purpose: Enable new conversations to automatically appear in both the Conversations page and the Overview dashboard without page refresh, while preventing API floods during rapid agent activity.

Output: Three modified composables with debounced WS refetch, newIds tracking, separate loading/refreshing states, and unit tests covering the key behaviors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-realtime-conversation-discovery/33-RESEARCH.md

@packages/frontend/src/composables/useConversations.ts
@packages/frontend/src/composables/useConversationBrowser.ts
@packages/frontend/src/composables/useAnalytics.ts
@packages/frontend/src/composables/useWebSocket.ts
@packages/frontend/tests/composables/useScrollTracker.test.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/frontend/src/composables/useWebSocket.ts:
```typescript
export function useWebSocket(): {
  state: Readonly<Ref<ConnectionState>>;
  reconnectAttempt: Readonly<Ref<number>>;
  on<T extends WebSocketEventType>(type: T, callback: EventCallback<T>): () => void;
};
// on() auto-cleans via onScopeDispose -- consumers don't need explicit cleanup
// _resetForTesting() available for test isolation
```

From packages/shared/src/types/websocket-events.ts:
```typescript
export type WebSocketEventType = 'conversation:created' | 'conversation:changed' | 'system:full-refresh';
export interface ConversationCreatedEvent {
  type: 'conversation:created';
  seq: number;
  conversationId: string;
  summary: { title: string | null; agent: string; project: string | null; createdAt: string; };
  timestamp: string;
}
```

From useConversations.ts (current return):
```typescript
return { data, loading, error, page, limit, sortField, sortOrder, setSort, setPage };
```

From useConversationBrowser.ts (current return):
```typescript
return { data, loading, error, page, limit, sortField, sortOrder, agent, project, searchQuery, setSort, setPage, setAgent, setProject, submitSearch, clearSearch, filterOptions };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add debounced WS refetch and newIds tracking to useConversations and useConversationBrowser</name>
  <files>
    packages/frontend/src/composables/useConversations.ts
    packages/frontend/src/composables/useConversationBrowser.ts
    packages/frontend/tests/composables/useConversations.test.ts
    packages/frontend/tests/composables/useConversationBrowser.test.ts
  </files>
  <behavior>
    - useConversations: on('conversation:created'), on('conversation:changed'), on('system:full-refresh') all trigger debouncedWsRefetch
    - useConversations: debouncedWsRefetch calls fetchConversations after 500ms, coalescing multiple rapid events into one call
    - useConversations: WS-triggered refetch does NOT set page.value = 1 (user stays on current page)
    - useConversations: initial dateRange/agentFilter watch is NOT debounced (immediate behavior preserved)
    - useConversations: newIds set correctly identifies rows present in new fetch but absent from previous fetch
    - useConversations: newIds is empty on initial load (previousIds starts empty, guard prevents marking everything as new)
    - useConversations: newIds auto-clears after 2000ms
    - useConversations: refreshing ref is true during WS-triggered fetch, loading ref is NOT set
    - useConversationBrowser: existing naive WS handlers replaced with debouncedWsRefetch (500ms)
    - useConversationBrowser: same newIds tracking, refreshing ref, and page-preservation behaviors
    - useConversationBrowser: debounce timer cleaned up via onScopeDispose
  </behavior>
  <action>
    **Tests first (RED):**

    Create `tests/composables/useConversations.test.ts`:
    - Mock `vue-router` (useRoute returns { query: {} }, useRouter returns { replace: vi.fn() })
    - Mock `useWebSocket` to capture `on()` callbacks: store them in a map, return them so tests can fire events
    - Mock `useDateRange` to return stable dateRange ref
    - Mock `fetch` globally to return a ConversationListResponse with known rows
    - Test: "subscribes to conversation:created, conversation:changed, system:full-refresh" -- verify on() called 3 times with those types
    - Test: "debounces WS refetch at 500ms" -- fire conversation:created, advance timer 300ms, fire again, advance timer 500ms, verify fetch called only once
    - Test: "does not reset page on WS refetch" -- set page.value = 3, fire conversation:created, advance 500ms, verify page.value === 3
    - Test: "tracks newIds after WS refetch" -- first fetch returns rows [A, B], fire WS event + advance, mock returns [C, A, B], verify newIds contains 'C'
    - Test: "does not mark rows as new on initial load" -- verify newIds is empty after initial fetch
    - Test: "uses refreshing ref for WS-triggered fetch" -- fire WS event, advance, verify refreshing was true during fetch

    Create `tests/composables/useConversationBrowser.test.ts`:
    - Same mocking pattern (vue-router, useWebSocket, useDateRange, fetch)
    - Test: "debounces WS refetch at 500ms" -- same coalescing test
    - Test: "does not reset page on WS refetch" -- same page preservation test
    - Test: "tracks newIds after WS refetch" -- same new row detection test
    - Test: "cleans up debounce timer on scope dispose" -- verify clearTimeout called

    **Implementation (GREEN):**

    Modify `useConversations.ts`:
    1. Import `useWebSocket` and `onScopeDispose`
    2. Add `const refreshing = ref(false)` alongside existing `loading`
    3. Modify `fetchConversations` to accept `isLive = false` parameter: if isLive, set `refreshing.value = true` instead of `loading.value = true`; in finally, set both to false
    4. Add `previousIds` ref (Set<string>) and `newIds` ref (Set<string>)
    5. After successful fetch in `fetchConversations`, call `trackNewRows()` which: skips if previousIds.size === 0 (initial load); computes diff of current vs previous IDs; sets newIds; updates previousIds; sets 2s timeout to clear newIds
    6. Add `debouncedWsRefetch()` with 500ms setTimeout that calls `fetchConversations(true)` -- uses a `wsDebounceTimer` variable
    7. Subscribe: `on('conversation:created', debouncedWsRefetch)`, `on('conversation:changed', debouncedWsRefetch)`, `on('system:full-refresh', debouncedWsRefetch)`
    8. Add `onScopeDispose` to clean wsDebounceTimer and newIds timeout
    9. Add `newIds` and `refreshing` to return object

    Modify `useConversationBrowser.ts`:
    1. Replace lines 177-180 (naive `on()` calls) with `debouncedWsRefetch()` pattern (same 500ms debounce)
    2. Add `refreshing` ref; modify `fetchConversations` to accept `isLive = false`
    3. Add `previousIds`/`newIds` tracking with same `trackNewRows()` logic
    4. Clean up wsDebounceTimer in the existing `onScopeDispose` block (merge with searchTimeout cleanup)
    5. Add `newIds` and `refreshing` to return object

    CRITICAL: The `debouncedWsRefetch` must NOT call `page.value = 1`. Only user-initiated actions (setSort, setAgent, setProject, submitSearch, clearSearch, dateRange watch) reset page.
  </action>
  <verify>
    <automated>cd packages/frontend && npx vitest run tests/composables/useConversations.test.ts tests/composables/useConversationBrowser.test.ts --reporter=verbose</automated>
  </verify>
  <done>
    - useConversations subscribes to all 3 WS event types with 500ms debounced refetch
    - useConversationBrowser uses 500ms debounced WS refetch instead of naive immediate refetch
    - Both composables expose newIds (Set of conversation IDs new since last fetch) and refreshing ref
    - Page is never reset to 1 during WS-triggered refetch
    - All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add debounced WS refetch to useAnalytics</name>
  <files>
    packages/frontend/src/composables/useAnalytics.ts
  </files>
  <action>
    Modify `useAnalytics.ts`:
    1. Import `onScopeDispose`
    2. Add `const refreshing = ref(false)` alongside existing `loading`
    3. Modify `fetchAll` to accept `isLive = false` parameter. If isLive, set `refreshing.value = true` instead of `loading.value = true`. In finally, set both to false.
    4. Add `debouncedWsRefetch()` with 500ms debounce timer calling `fetchAll(true)`
    5. Replace lines 63-65 (naive `on()` calls) with debounced versions:
       ```
       on('conversation:changed', debouncedWsRefetch);
       on('conversation:created', debouncedWsRefetch);
       on('system:full-refresh', debouncedWsRefetch);
       ```
    6. Add `onScopeDispose` to clear the debounce timer
    7. Add `refreshing` to the return object

    This prevents the overview KPI cards and charts from making 3 separate rapid API calls (overview + timeseries + model-distribution) x N events during burst activity.
  </action>
  <verify>
    <automated>cd packages/frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - useAnalytics uses 500ms debounced WS refetch instead of naive immediate refetch
    - refreshing ref exposed for UI consumers
    - All existing tests still pass (no regressions)
  </done>
</task>

</tasks>

<verification>
1. `cd packages/frontend && npx vitest run --reporter=verbose` -- all tests pass
2. `cd packages/frontend && npx tsc --noEmit` -- no type errors
3. Verify useConversations.ts contains `on('conversation:created'` subscription
4. Verify useConversationBrowser.ts contains `debouncedWsRefetch` replacing naive handler
5. Verify useAnalytics.ts contains `debouncedWsRefetch` replacing naive handler
</verification>

<success_criteria>
- Both list composables subscribe to WS events with 500ms debounced refetch
- Page state preserved during WS-triggered refetches
- newIds tracking exposes which rows are new for UI highlighting
- Separate loading/refreshing refs prevent full-page spinner on live updates
- All unit tests green, no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/33-realtime-conversation-discovery/33-01-SUMMARY.md`
</output>
