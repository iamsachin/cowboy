---
phase: 17-cost-calculation-fixes
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - packages/frontend/src/pages/ConversationDetailPage.vue
  - packages/frontend/src/components/ConversationTable.vue
  - packages/frontend/src/components/ProjectTable.vue
  - packages/frontend/src/components/CostChart.vue
  - packages/frontend/src/components/AgentOverlayChart.vue
  - packages/frontend/src/components/AssistantGroupCard.vue
autonomous: true
requirements: [COST-03, COST-04, COST-05]

must_haves:
  truths:
    - "Conversation header cost shows meaningful precision for sub-cent costs (not $0.00)"
    - "Token totals show input + output as primary total, cache tokens shown separately (no double-counting)"
    - "Token display shows input + cacheRead as effective input count"
    - "All cost displays use the shared formatCost() from format-tokens.ts"
  artifacts:
    - path: "packages/frontend/src/pages/ConversationDetailPage.vue"
      provides: "Fixed header cost formatting, fixed token totals, fixed token display"
    - path: "packages/frontend/src/components/ConversationTable.vue"
      provides: "Uses shared formatCost()"
    - path: "packages/frontend/src/components/ProjectTable.vue"
      provides: "Uses shared formatCost()"
  key_links:
    - from: "packages/frontend/src/pages/ConversationDetailPage.vue"
      to: "packages/frontend/src/utils/format-tokens.ts"
      via: "import { formatCost }"
      pattern: "import.*formatCost.*from.*format-tokens"
    - from: "packages/frontend/src/components/ConversationTable.vue"
      to: "packages/frontend/src/utils/format-tokens.ts"
      via: "import { formatCost }"
      pattern: "import.*formatCost.*from.*format-tokens"
---

<objective>
Fix frontend cost display: proper precision formatting, correct token totals, and unified formatCost() usage.

Purpose: The header shows "$0.00" for sub-cent costs (COST-03), token totals double-count cache tokens (COST-04), and token display shows misleadingly low input counts by excluding cache reads (COST-05). All cost displays must use the shared formatCost() created in Plan 01.
Output: All frontend cost displays corrected and unified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-cost-calculation-fixes/17-01-SUMMARY.md

@packages/frontend/src/pages/ConversationDetailPage.vue
@packages/frontend/src/components/ConversationTable.vue
@packages/frontend/src/components/ProjectTable.vue
@packages/frontend/src/components/CostChart.vue
@packages/frontend/src/components/AgentOverlayChart.vue
@packages/frontend/src/components/AssistantGroupCard.vue
@packages/frontend/src/utils/format-tokens.ts

<interfaces>
<!-- From Plan 01 output (format-tokens.ts) -->
```typescript
export function formatCost(cost: number): string;
export function formatTurnCost(cost: number): string;
export function formatTokenCount(count: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix token totals and display in ConversationDetailPage</name>
  <files>packages/frontend/src/pages/ConversationDetailPage.vue</files>
  <action>
    **COST-03 fix (line 170-172):**
    Remove the local `formatCost()` function that uses `toFixed(2)`. Import `formatCost` from `../utils/format-tokens`. The template already calls `formatCost()` on lines 84 and 89 — just changing the import will fix the precision.

    **COST-04 fix (line 151-155):**
    Change `totalTokens` computed property from:
    ```
    s.inputTokens + s.outputTokens + s.cacheReadTokens + s.cacheCreationTokens
    ```
    to:
    ```
    s.inputTokens + s.outputTokens
    ```
    This is the primary total (no double-counting). Cache tokens are already shown separately in the token breakdown section.

    **COST-05 fix:**
    In the template where input tokens are displayed, change the displayed value from `s.inputTokens` to `s.inputTokens + s.cacheReadTokens` and update the label to "Input (incl. cache)" or show them as "X in (Y cached)". Check how the template currently renders token breakdown and update accordingly. The key insight: cacheReadTokens ARE input tokens served from cache, so effective input = inputTokens + cacheReadTokens.

    Look at the template section of ConversationDetailPage.vue to find where individual token counts are displayed and update the input display.
  </action>
  <verify>
    <automated>cd packages/frontend && grep -n "formatCost" src/pages/ConversationDetailPage.vue | head -5 && grep -n "totalTokens" src/pages/ConversationDetailPage.vue | head -5</automated>
  </verify>
  <done>
    - Header cost uses shared formatCost() with proper precision (sub-cent costs show actual value, not $0.00)
    - totalTokens = inputTokens + outputTokens only (no cache double-counting)
    - Input token display includes cacheReadTokens for accurate count
    - No local formatCost function remains in ConversationDetailPage.vue
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace all local formatCost implementations with shared utility</name>
  <files>packages/frontend/src/components/ConversationTable.vue, packages/frontend/src/components/ProjectTable.vue, packages/frontend/src/components/CostChart.vue, packages/frontend/src/components/AgentOverlayChart.vue</files>
  <action>
    **COST-06 wiring (all remaining components):**

    **ConversationTable.vue (~line 148-153, 165-167):**
    - Remove the local `costFormatter` Intl.NumberFormat instance and the local `formatCost()` function
    - Import `formatCost` from `../../utils/format-tokens`
    - Template already calls `formatCost(row.cost)` on line 65 — just the import change needed

    **ProjectTable.vue (~line 187-188):**
    - Remove the local `formatCost()` function that uses `n.toFixed(2)`
    - Import `formatCost` from `../../utils/format-tokens`
    - Template already calls `formatCost(row.totalCost)` on line 45

    **CostChart.vue (~line 253):**
    - Replace inline `\`$${Number(value).toFixed(2)}\`` in the chart axis callback with `formatCost(Number(value))`
    - Import `formatCost` from `../../utils/format-tokens`

    **AgentOverlayChart.vue (~line 115):**
    - Replace inline `\`$${n.toFixed(2)}\`` in the cost metric formatter with `formatCost(n)`
    - Import `formatCost` from `../../utils/format-tokens`

    **AssistantGroupCard.vue** already uses `formatTurnCost` from format-tokens.ts — no change needed (formatTurnCost is fine for per-turn display).

    After all changes, verify NO component has a local cost formatting function or inline toFixed(2) for cost values.
  </action>
  <verify>
    <automated>cd packages/frontend && grep -rn "toFixed(2)" src/components/ src/pages/ | grep -i cost | grep -v node_modules | grep -v ".test." && echo "--- Should show NO results above ---" && grep -rn "import.*formatCost.*format-tokens" src/ | wc -l | xargs echo "Components using shared formatCost:"</automated>
  </verify>
  <done>
    - No local formatCost() functions remain in any component
    - No inline toFixed(2) used for cost formatting in any component
    - All cost displays import formatCost from utils/format-tokens.ts
    - CostChart and AgentOverlayChart axis labels use formatCost()
  </done>
</task>

</tasks>

<verification>
- `grep -rn "function formatCost" packages/frontend/src/` returns ONLY the one in format-tokens.ts
- `grep -rn "toFixed(2)" packages/frontend/src/ | grep -iv "token\|chart.*value\|test"` returns no cost-related hits
- `grep -n "cacheReadTokens\|cacheCreation" packages/frontend/src/pages/ConversationDetailPage.vue` shows cache tokens NOT in totalTokens sum
- Build passes: `cd packages/frontend && npx vue-tsc --noEmit` (or equivalent type check)
</verification>

<success_criteria>
- Sub-cent costs display with proper precision (e.g., "$0.003" not "$0.00")
- Token total is input + output only; cache shown separately
- Input token count includes cache reads for accuracy
- Single formatCost() used everywhere — no local implementations remain
</success_criteria>

<output>
After completion, create `.planning/phases/17-cost-calculation-fixes/17-02-SUMMARY.md`
</output>
