---
phase: 10-data-grouping-foundation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - packages/frontend/src/components/TurnCard.vue
  - packages/frontend/src/components/ToolCallRow.vue
  - packages/frontend/src/components/ConversationDetail.vue
  - packages/frontend/src/components/ChatMessage.vue
autonomous: false

requirements: [GROUP-01, GROUP-02]

must_haves:
  truths:
    - "Assistant turns render as subtle card containers with base-200 background and thin border"
    - "User messages keep existing chat-end bubble styling unchanged"
    - "Assistant output text is visible directly in the card without expanding"
    - "Tool calls display as compact rows (icon + name + status + duration) inside the turn card"
    - "Thinking content appears inside the turn card as a collapsed details/summary section above the text"
    - "Conversation still feels like a chat -- user bubbles right, assistant cards left, linear flow"
  artifacts:
    - path: "packages/frontend/src/components/TurnCard.vue"
      provides: "Assistant turn card with thinking + text + tool call rows"
      min_lines: 40
    - path: "packages/frontend/src/components/ToolCallRow.vue"
      provides: "Compact single-line tool call display"
      min_lines: 15
    - path: "packages/frontend/src/components/ConversationDetail.vue"
      provides: "Refactored to render Turn[] instead of flat timeline"
      contains: "groupTurns"
    - path: "packages/frontend/src/components/ChatMessage.vue"
      provides: "Simplified to user messages only, imports from content-parser.ts"
      contains: "content-parser"
  key_links:
    - from: "packages/frontend/src/components/ConversationDetail.vue"
      to: "packages/frontend/src/composables/useGroupedTurns.ts"
      via: "import groupTurns, computed Turn[]"
      pattern: "groupTurns.*messages.*toolCalls"
    - from: "packages/frontend/src/components/TurnCard.vue"
      to: "packages/frontend/src/utils/content-parser.ts"
      via: "import parseContent, formatTime"
      pattern: "import.*parseContent.*content-parser"
    - from: "packages/frontend/src/components/TurnCard.vue"
      to: "packages/frontend/src/components/ToolCallRow.vue"
      via: "v-for rendering tool calls"
      pattern: "ToolCallRow.*v-for"
---

<objective>
Build TurnCard and ToolCallRow components, refactor ConversationDetail to render grouped turns, and simplify ChatMessage for user-only messages.

Purpose: Wire the grouping algorithm (Plan 01) into the UI. Users see assistant turns as subtle cards with text always visible (GROUP-02) and tool calls as compact rows inside cards (GROUP-01 rendering). The conversation maintains its chat feel with user bubbles on the right and assistant cards on the left.

Output: Working conversation detail view with grouped turns replacing the flat timeline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-data-grouping-foundation/10-CONTEXT.md
@.planning/phases/10-data-grouping-foundation/10-RESEARCH.md
@.planning/phases/10-data-grouping-foundation/10-01-SUMMARY.md

@packages/frontend/src/components/ConversationDetail.vue
@packages/frontend/src/components/ChatMessage.vue
@packages/frontend/src/components/ToolCallCard.vue
@packages/frontend/src/pages/ConversationDetailPage.vue
@packages/frontend/src/utils/content-sanitizer.ts

<interfaces>
<!-- From Plan 01 output: groupTurns function and types -->
From packages/frontend/src/composables/useGroupedTurns.ts:
```typescript
export interface UserTurn {
  type: 'user';
  message: MessageRow;
}

export interface AssistantTurn {
  type: 'assistant';
  message: MessageRow;
  toolCalls: ToolCallRow[];
}

export type Turn = UserTurn | AssistantTurn;

export function groupTurns(messages: MessageRow[], toolCalls: ToolCallRow[]): Turn[];
```

From packages/frontend/src/utils/content-parser.ts:
```typescript
export interface ContentBlock {
  type: 'text' | 'code';
  content: string;
  language?: string;
}

export function parseContent(raw: string): ContentBlock[];
export function formatTime(isoString: string): string;
```

From @cowboy/shared:
```typescript
export interface MessageRow {
  id: string;
  role: string;
  content: string | null;
  thinking: string | null;
  createdAt: string;
  model: string | null;
}

export interface ToolCallRow {
  id: string;
  messageId: string;
  name: string;
  input: unknown;
  output: unknown;
  status: string | null;
  duration: number | null;
  createdAt: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TurnCard and ToolCallRow components</name>
  <files>packages/frontend/src/components/TurnCard.vue, packages/frontend/src/components/ToolCallRow.vue</files>
  <action>
    Create ToolCallRow.vue -- a compact single-line row for tool calls:
    - Layout: flex row with icon + name (truncated) + status badge + duration (ml-auto)
    - Use Wrench icon from lucide-vue-next (w-3.5 h-3.5 text-info)
    - Status badge: DaisyUI `badge badge-xs`, badge-success for "completed", badge-warning for others, badge-ghost for null/unknown
    - Duration: show `Xms` in text-base-content/50 if duration is not null
    - Styling: `text-xs py-1 px-2 rounded bg-base-300/50`
    - Props: `{ toolCall: ToolCallRow }`

    Create TurnCard.vue -- the assistant turn card container:
    - Outer div: `bg-base-200 border border-base-300 rounded-lg p-4 max-w-[85%]` (per discretion: 85% matches existing ChatMessage constraint)
    - Minimal header: role label "Assistant" + formatted timestamp in text-xs text-base-content/50 mb-2
    - Thinking section (above assistant text): Use native `<details>/<summary>` (NOT DaisyUI checkbox collapse -- STATE.md decision about nesting bugs). Show only if `turn.message.thinking` is truthy. Summary shows Brain icon + "Thinking" label. Content is pre-wrapped text-xs.
    - Assistant text body: Use `parseContent` from `content-parser.ts` to render text blocks and CodeBlock components. Text is always visible (GROUP-02). Use `stripXmlTags` from `content-sanitizer.ts` for the same cleaning. If content is null/empty, skip text section but still show tool calls.
    - Tool call list: Show all tool calls (no cap per discretion) below text with `mt-3 space-y-1`. Render ToolCallRow for each. Only show section if `turn.toolCalls.length > 0`.
    - Props: `{ turn: AssistantTurn }` imported from useGroupedTurns.ts
    - ALL assistant turns get cards (per discretion: consistent with Phase 11 collapsible headers coming next; text-only turns also get cards)
    - Import CodeBlock.vue for code block rendering inside assistant text

    Important: Do NOT use DaisyUI `chat-bubble` class inside the card. Assistant text renders directly as plain text blocks (per user decision).
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy && npx tsc --noEmit --project packages/frontend/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>TurnCard.vue renders assistant turn with thinking (collapsed details/summary) + text (always visible) + tool call rows. ToolCallRow.vue shows compact icon + name + status + duration line.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ConversationDetail and ChatMessage</name>
  <files>packages/frontend/src/components/ConversationDetail.vue, packages/frontend/src/components/ChatMessage.vue</files>
  <action>
    Refactor ConversationDetail.vue:
    - Remove the flat timeline logic (TimelineItem interface, isEmptyMessage function, timeline computed)
    - Import `groupTurns` and `Turn` from `../composables/useGroupedTurns`
    - Import TurnCard from `./TurnCard.vue`
    - Create `turns` computed: `computed(() => groupTurns(props.messages, props.toolCalls))`
    - Template: iterate `turns` with v-for, key by `turn.message.id`
      - `v-if="turn.type === 'user'"` -> render ChatMessage with `:message="turn.message"`
      - `v-else` -> render TurnCard with `:turn="turn"`
    - Remove ToolCallCard import (no longer used in this component)
    - Remove `stripXmlTags` import -- empty message handling now lives in TurnCard (it skips text rendering if content is empty, but still shows tool calls)
    - Layout: `space-y-3` for turn spacing (slightly more than current space-y-2 to accommodate cards)

    Simplify ChatMessage.vue:
    - This component now only renders USER messages (assistant rendering moved to TurnCard)
    - Remove the thinking collapsible section entirely (thinking now lives in TurnCard)
    - Remove the assistant-specific conditional classes (no more `chat-start` for assistant)
    - Keep: `chat chat-end ml-auto max-w-[85%]` layout for user messages
    - Replace inline `parseContent` function with import from `../utils/content-parser`
    - Replace inline `formatTime` function with import from `../utils/content-parser`
    - Remove Brain icon import (no longer needed)
    - Keep CodeBlock import for user messages that contain code blocks
    - The chat-header should show "You" + timestamp (already does this for user)
    - Keep the `chat-bubble chat-bubble-primary` styling for user messages

    After refactoring, verify the existing tests still pass and the TypeScript compiles.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/packages/frontend && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>ConversationDetail renders grouped Turn[] with TurnCard for assistants and ChatMessage for users. ChatMessage is simplified to user-only rendering. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of grouped conversation view</name>
  <action>Verify the grouped conversation view visually. All automation is complete from Tasks 1-2; this checkpoint confirms the visual result.</action>
  <verify>Visual inspection per how-to-verify steps below</verify>
  <done>User confirms grouped conversation view looks correct, feels like a chat, and assistant text is visible without expanding</done>
  <what-built>Grouped conversation view replacing the flat timeline. Assistant turns now appear as subtle cards (base-200 background, thin border) containing thinking (collapsed), assistant text (always visible), and compact tool call rows. User messages remain as chat bubbles on the right. The conversation maintains a linear chat feel.</what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Open a conversation that has assistant messages with tool calls (e.g., any Claude Code conversation)
    3. Verify:
       a. User messages appear as blue bubbles on the right (unchanged from before)
       b. Assistant messages appear as subtle cards (gray background, thin border) on the left
       c. Assistant text is visible directly in the card without clicking anything (GROUP-02)
       d. Tool calls appear as compact rows below the assistant text (icon + name + status badge + duration)
       e. If the assistant has thinking content, there is a collapsed "Thinking" section above the text (click to expand)
       f. Consecutive assistant messages appear as separate cards (not merged)
       g. The conversation still feels like a chat flow, not a structured log
    4. Open a conversation with many tool calls -- verify all show (no truncation)
    5. Open a conversation with code blocks in assistant responses -- verify code blocks render correctly inside cards
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- Unit tests pass: `cd packages/frontend && npx vitest run`
- TypeScript compiles: `npx tsc --noEmit --project packages/frontend/tsconfig.json`
- Visual: grouped conversation view renders correctly in browser
</verification>

<success_criteria>
- Assistant turns display as grouped cards with text + thinking + tool call rows (GROUP-01)
- Assistant text is visible without expanding (GROUP-02)
- User messages unchanged as chat bubbles
- Orphan tool calls and consecutive assistant messages handled correctly (from Plan 01 algorithm)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/10-data-grouping-foundation/10-02-SUMMARY.md`
</output>
