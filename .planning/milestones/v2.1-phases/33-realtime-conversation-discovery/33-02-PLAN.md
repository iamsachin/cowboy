---
phase: 33-realtime-conversation-discovery
plan: 02
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - packages/frontend/src/components/ConversationTable.vue
  - packages/frontend/src/components/ConversationBrowser.vue
autonomous: false
requirements:
  - PUSH-04
  - PUSH-05

must_haves:
  truths:
    - "New conversations appearing in the Conversations page have a brief green fade highlight"
    - "New conversations appearing in the Overview table have a brief green fade highlight"
    - "The full-page loading spinner only shows on initial load, not on live WS-triggered refetches"
    - "Pagination, sort order, and filters remain unchanged when new conversations appear"
  artifacts:
    - path: "packages/frontend/src/components/ConversationTable.vue"
      provides: "Row highlight for new conversations, refreshing-aware loading state"
      contains: "row-highlight"
    - path: "packages/frontend/src/components/ConversationBrowser.vue"
      provides: "Row highlight for new conversations, refreshing-aware loading state"
      contains: "row-highlight"
  key_links:
    - from: "packages/frontend/src/components/ConversationTable.vue"
      to: "useConversations"
      via: "newIds from composable"
      pattern: "newIds"
    - from: "packages/frontend/src/components/ConversationBrowser.vue"
      to: "useConversationBrowser"
      via: "newIds from composable"
      pattern: "newIds"
---

<objective>
Wire newIds and refreshing state from composables into both table components for row highlight animation and loading state refinement.

Purpose: Provide visual feedback when new conversations appear and prevent jarring full-page spinners during live updates.

Output: Both table components show a brief green fade on new rows and avoid full spinner during WS-triggered refetches.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-realtime-conversation-discovery/33-RESEARCH.md
@.planning/phases/33-realtime-conversation-discovery/33-01-SUMMARY.md

@packages/frontend/src/components/ConversationTable.vue
@packages/frontend/src/components/ConversationBrowser.vue

<interfaces>
<!-- Composable return types after Plan 01 modifications -->

From useConversations (after 33-01):
```typescript
return {
  data, loading, refreshing, error, page, limit, sortField, sortOrder,
  setSort, setPage, newIds,  // newIds: Ref<Set<string>>
};
```

From useConversationBrowser (after 33-01):
```typescript
return {
  data, loading, refreshing, error, page, limit, sortField, sortOrder,
  agent, project, searchQuery, setSort, setPage, setAgent, setProject,
  submitSearch, clearSearch, filterOptions, newIds,  // newIds: Ref<Set<string>>
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire newIds highlight and refreshing state into both table components</name>
  <files>
    packages/frontend/src/components/ConversationTable.vue
    packages/frontend/src/components/ConversationBrowser.vue
  </files>
  <action>
    **ConversationTable.vue:**
    1. Destructure `newIds` and `refreshing` from `useConversations(agentRef)` (line 152)
    2. On the data `<tr>` element (line 40-46), add dynamic class: `:class="{ 'row-highlight': newIds.has(row.id) }"`
    3. Update loading state logic: the full spinner (line 26-29) should only show when `loading && !data` (already correct). The existing loading overlay does not exist in this component -- no overlay needed since this is the overview table; just ensure the `loading` ref is only true on initial load (Plan 01 handles this).
    4. Add CSS keyframes in `<style scoped>` block (alongside existing pulse-dot styles):
       ```css
       .row-highlight {
         animation: row-enter 2s ease-out;
       }
       @keyframes row-enter {
         0% { background-color: oklch(0.85 0.1 142 / 0.3); }
         100% { background-color: transparent; }
       }
       ```

    **ConversationBrowser.vue:**
    1. Destructure `newIds` and `refreshing` from `useConversationBrowser()` (line 200-216)
    2. On the data `<tr>` element (line 102-105), add dynamic class: `:class="{ 'row-highlight': newIds.has(row.id) }"`
    3. Update the loading overlay (line 55-59): change condition from `loading && data` to `(loading || refreshing) && data` -- but actually, since Plan 01 separates loading/refreshing, the overlay should show only on `loading && data` (user-initiated filter/sort fetches). For WS-triggered refetches, NO overlay -- the table stays fully interactive. Keep the condition as `loading && data` (unchanged). This means live WS refetches are invisible except for the row highlight, which is ideal UX.
    4. Add same CSS keyframes in `<style scoped>`:
       ```css
       .row-highlight {
         animation: row-enter 2s ease-out;
       }
       @keyframes row-enter {
         0% { background-color: oklch(0.85 0.1 142 / 0.3); }
         100% { background-color: transparent; }
       }
       ```

    CRITICAL: Do NOT modify any loading conditional logic beyond what is described. The existing `loading && !data` for initial spinner and `loading && data` for overlay are correct. WS-triggered refetches use `refreshing` (not `loading`), so they naturally bypass all loading indicators.
  </action>
  <verify>
    <automated>cd packages/frontend && npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <done>
    - ConversationTable.vue applies row-highlight class to rows whose ID is in newIds
    - ConversationBrowser.vue applies row-highlight class to rows whose ID is in newIds
    - Both components have the CSS @keyframes row-enter animation (2s green fade)
    - Loading spinners only show during initial/user-initiated loads, not WS refetches
    - TypeScript compiles with no errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify realtime conversation discovery end-to-end</name>
  <action>
    Human verifies that new conversations automatically appear in both the Conversations page and Overview dashboard with visual highlight animation, preserved pagination, and no full-page spinner during live updates.
  </action>
  <verify>User confirms visual behavior matches expectations</verify>
  <done>User types "approved" confirming all behaviors work correctly</done>
  <what-built>
    Realtime conversation discovery: new conversations automatically appear in both the Conversations page and the Overview dashboard when an agent creates them, with a brief green highlight animation. Pagination, sort, and filters are preserved. No full-page spinner during live updates.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd packages/frontend && npm run dev` (and backend if not already running)
    2. Open the Conversations page in the browser
    3. Start a Claude Code or Cursor agent session to trigger a new conversation
    4. Verify: new conversation row appears without page refresh, with brief green fade highlight
    5. Navigate to page 2 of conversations, trigger another new conversation -- verify page stays on 2 and pagination count updates
    6. Open the Overview page
    7. Trigger another new conversation -- verify it appears in the Overview table with green highlight
    8. Verify KPI cards also update (this was already working via useAnalytics)
    9. Rapid test: trigger multiple quick conversations (e.g., via subagents) -- verify no API flood (single debounced refetch)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd packages/frontend && npx tsc --noEmit` -- no type errors
2. `cd packages/frontend && npx vitest run --reporter=verbose` -- all tests pass
3. Browser: new conversation appears in Conversations page without refresh
4. Browser: new conversation appears in Overview table without refresh
5. Browser: pagination/sort/filter preserved during live updates
6. Browser: green highlight animation on new rows, fades after 2 seconds
</verification>

<success_criteria>
- New conversations auto-appear in both Conversations and Overview pages (PUSH-04, PUSH-05)
- Brief green fade animation highlights new rows
- No full-page spinner during live WS updates
- Pagination, sort order, and filters preserved
- User confirms visual behavior via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/33-realtime-conversation-discovery/33-02-SUMMARY.md`
</output>
