---
phase: 40-file-watcher-desktop-chrome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/tauri.conf.json
  - packages/frontend/vite.config.ts
  - src-tauri/src/server.rs
  - src-tauri/src/watcher.rs
  - src-tauri/src/settings.rs
  - src-tauri/src/ingestion/mod.rs
  - src-tauri/src/lib.rs
  - src-tauri/src/schema.sql
  - packages/frontend/src/pages/SettingsPage.vue
autonomous: true
requirements: [WATCH-01, WATCH-02]

must_haves:
  truths:
    - "File changes in Claude Code log directory trigger automatic ingestion within seconds"
    - "File changes in Cursor globalStorage directory trigger automatic ingestion within seconds"
    - "Rapid successive file changes are debounced (1s Claude, 3s Cursor) -- not processed individually"
    - "Changing agent paths or toggles in Settings restarts the file watcher with new configuration"
    - "POST /settings/refresh-db triggers a full ingestion run"
    - "Server listens on port 8123 by default"
    - "Port is configurable via Settings page (stored in settings table)"
    - "Changing port shows toast: 'Port changed. Restart app to apply.'"
  artifacts:
    - path: "src-tauri/src/watcher.rs"
      provides: "File watcher module with notify + tokio debounce"
      exports: ["FileWatcherHandle", "start_watcher"]
      min_lines: 100
    - path: "src-tauri/src/server.rs"
      provides: "Axum server reading port from settings table (default 8123) with watcher handle in AppState"
      contains: "8123"
    - path: "src-tauri/src/settings.rs"
      provides: "Watcher restart on agent settings change, refresh-db wired, port field in responses"
      contains: "run_ingestion"
    - path: "src-tauri/src/schema.sql"
      provides: "Settings table with server_port column"
      contains: "server_port"
  key_links:
    - from: "src-tauri/src/watcher.rs"
      to: "src-tauri/src/ingestion/mod.rs"
      via: "calls run_ingestion on debounced events"
      pattern: "run_ingestion"
    - from: "src-tauri/src/settings.rs"
      to: "src-tauri/src/watcher.rs"
      via: "restarts watcher on path/toggle change"
      pattern: "start_watcher"
    - from: "src-tauri/src/server.rs"
      to: "src-tauri/src/watcher.rs"
      via: "spawns watcher on server start"
      pattern: "start_watcher"
---

<objective>
Create the notify-based file watcher module, wire it into the server and settings, change the server port to 8123 (configurable via Settings), and update all configuration to match.

Purpose: File watching is the core feature that makes Cowboy a "set and forget" desktop app -- new conversations appear automatically without manual refresh.
Output: Working file watcher that triggers ingestion on JSONL/vscdb changes, with debounce and settings-aware restart. Port configurable via Settings page with toast notification.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-file-watcher-desktop-chrome/40-CONTEXT.md
@.planning/phases/40-file-watcher-desktop-chrome/40-RESEARCH.md

@src-tauri/src/server.rs
@src-tauri/src/settings.rs
@src-tauri/src/ingestion/mod.rs
@src-tauri/src/schema.sql
@src-tauri/Cargo.toml
@src-tauri/tauri.conf.json
@packages/frontend/vite.config.ts
@packages/frontend/src/pages/SettingsPage.vue

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src-tauri/src/server.rs:
```rust
pub struct AppStateInner {
    pub db: Connection,
    pub tx: broadcast::Sender<String>,
}
pub type AppState = Arc<AppStateInner>;
```

From src-tauri/src/ingestion/mod.rs:
```rust
type SharedStatus = Arc<Mutex<IngestionStatus>>;
fn new_shared_status() -> SharedStatus;
pub fn spawn_auto_ingest(state: AppState);
pub async fn run_ingestion(state: &AppState, status: SharedStatus) -> Result<IngestionStats, Box<dyn std::error::Error + Send + Sync>>;
```

From src-tauri/src/websocket.rs:
```rust
pub fn broadcast_event(state: &AppState, event_type: &str, payload: Option<Value>);
```

From src-tauri/src/settings.rs:
```rust
// update_agent handler broadcasts "settings:changed" after DB update
// refresh_db currently returns 501 NotImplemented
// SettingsResponse struct has fields: id, claude_code_path, claude_code_enabled, cursor_path, cursor_enabled, sync_*, last_sync_*
// settings table: id, claude_code_path, claude_code_enabled, cursor_path, cursor_enabled, sync_enabled, sync_url, sync_frequency, sync_categories, last_sync_at, last_sync_error, last_sync_success, sync_cursor
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Watcher module skeleton with unit tests for path classification and debounce</name>
  <files>src-tauri/src/watcher.rs, src-tauri/src/lib.rs, src-tauri/Cargo.toml</files>
  <behavior>
    - Test: classify_event with .jsonl path returns AgentKind::ClaudeCode
    - Test: classify_event with state.vscdb path returns AgentKind::Cursor
    - Test: classify_event with .txt path returns None (ignored)
    - Test: classify_event with directory path returns None
    - Test: debounce_key for Claude returns "claude-code", for Cursor returns "cursor"
    - Test: default_debounce_duration for ClaudeCode is 1s, for Cursor is 3s
  </behavior>
  <action>
1. **Cargo.toml:** Add `notify = "8"` to dependencies. Add `"tray-icon"` and `"image-png"` to tauri features list (needed by Plan 02, but must be in Cargo.toml now since Plan 02 depends on this plan).

2. **watcher.rs (NEW):** Create the file with:
   - `pub enum AgentKind { ClaudeCode, Cursor }` with derive Debug, Clone, PartialEq
   - `pub fn classify_event(path: &std::path::Path) -> Option<AgentKind>` -- checks extension for `.jsonl` (ClaudeCode) or filename `state.vscdb` (Cursor), returns None for anything else
   - `impl AgentKind { pub fn debounce_key(&self) -> &'static str` and `pub fn debounce_duration(&self) -> std::time::Duration` }
   - `pub struct FileWatcherHandle` (empty for now, will be filled in Task 1)
   - `#[cfg(test)] mod tests` with the unit tests described in behavior

3. **lib.rs:** Add `mod watcher;` declaration alongside existing mod declarations.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/src-tauri && cargo test watcher -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 6 watcher unit tests pass
    - Path classification correctly identifies .jsonl, state.vscdb, and ignores other files
    - Debounce durations are 1s (Claude) and 3s (Cursor)
    - `mod watcher` declared in lib.rs
    - notify crate added to Cargo.toml
  </done>
</task>

<task type="auto">
  <name>Task 1: Config updates, port change, and full watcher implementation</name>
  <files>src-tauri/tauri.conf.json, packages/frontend/vite.config.ts, src-tauri/src/server.rs, src-tauri/src/watcher.rs, src-tauri/src/ingestion/mod.rs, src-tauri/src/schema.sql, src-tauri/src/settings.rs</files>
  <action>
1. **schema.sql:** Add `server_port INTEGER NOT NULL DEFAULT 8123` column to the settings table CREATE TABLE statement (add after `sync_cursor TEXT` line).

2. **tauri.conf.json:** Update CSP `connect-src` to replace all `:3001` references with `:8123`. Keep all other CSP values identical.

3. **vite.config.ts:** Change proxy target from `http://127.0.0.1:3001` to `http://127.0.0.1:8123`.

4. **server.rs:**
   - Add `watcher: tokio::sync::Mutex<Option<FileWatcherHandle>>` field to `AppStateInner` (import from `crate::watcher`).
   - Read port from settings table before binding: `db.call(|conn| { conn.query_row("SELECT server_port FROM settings WHERE id = 1", [], |r| r.get::<_, i64>(0)).unwrap_or(8123) })`. Fall back to 8123 if no row exists.
   - Change bind address to use the port read from settings (format `127.0.0.1:{port}`) and update the println message to show actual port.
   - Replace the `ingestion::spawn_auto_ingest(shared_state)` call with file watcher initialization:
     - Read settings from DB to get paths and enabled flags (use `db.call(|conn| ...)`)
     - Call `watcher::start_watcher(state.clone(), settings)` and store the handle in `state.watcher`
   - Export `AppStateInner` and `AppState` as before.

5. **watcher.rs:** Expand the skeleton from Task 0 into the full implementation:
   - Fill `pub struct FileWatcherHandle` with `_watcher: RecommendedWatcher` and `shutdown_tx: Option<tokio::sync::oneshot::Sender<()>>`.
   - `pub fn start_watcher(state: AppState, claude_path: Option<String>, cursor_path: Option<String>, claude_enabled: bool, cursor_enabled: bool) -> Result<FileWatcherHandle, notify::Error>`:
     - Create `RecommendedWatcher` with `mpsc::channel::<notify::Event>(100)` bridge to tokio.
     - Watch Claude Code path with `RecursiveMode::Recursive` if claude_enabled (default `~/.claude/projects/`, depth 5 -- notify's recursive mode handles this).
     - Watch Cursor path with `RecursiveMode::NonRecursive` if cursor_enabled (default `~/Library/Application Support/Cursor/User/globalStorage/`).
     - Spawn tokio task with debounce loop using `tokio::select!`:
       - Use `classify_event()` from Task 0 to determine agent kind.
       - Claude timer: 1 second debounce, triggers on .jsonl file events.
       - Cursor timer: 3 second debounce, triggers on state.vscdb events.
       - Shutdown receiver: breaks loop on signal.
     - On timer expiry, call `trigger_ingestion(&state).await` which creates a `new_shared_status()` and calls `run_ingestion()`. Log errors but don't crash.
   - Use `dirs::home_dir()` for default path expansion (already using `dirs` crate in Cargo.toml).
   - Keep existing unit tests from Task 0 intact.

6. **ingestion/mod.rs:** Make `new_shared_status()` and `SharedStatus` type `pub` so watcher.rs can use them. Keep `spawn_auto_ingest` but it will no longer be called from server.rs.

7. **settings.rs -- port field:**
   - Add `server_port: i64` to `SettingsResponse` struct (with serde rename to `serverPort`).
   - Update `map_settings_row` to read the `server_port` column (add as the next positional index after sync_cursor).
   - Update all SELECT queries in settings.rs to include `server_port` in the column list.
   - Update `get_or_seed_settings` INSERT to include `server_port` with default 8123.
   - Add `PUT /api/settings/port` route with a `PortSettingsBody { server_port: i64 }` struct. Handler updates `settings SET server_port = ?1 WHERE id = 1`, broadcasts `"settings:changed"` event, returns updated settings.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/src-tauri && cargo build 2>&1 | tail -5 && cargo test watcher -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <done>
    - `cargo build` succeeds with notify dependency and watcher module
    - All Task 0 unit tests still pass
    - Server reads port from settings table (default 8123)
    - Vite proxies to :8123
    - CSP allows :8123 connections
    - Watcher module compiles with full debounce logic using classify_event()
    - settings.rs includes server_port in responses and has PUT /api/settings/port endpoint
    - schema.sql has server_port column
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings integration -- watcher restart, refresh-db, and port UI</name>
  <files>src-tauri/src/settings.rs, packages/frontend/src/pages/SettingsPage.vue</files>
  <action>
1. **Wire refresh-db:** Replace the 501 stub with a real handler. The handler should:
   - Accept `State(state): State<AppState>` parameter.
   - Spawn a tokio task that calls `crate::ingestion::run_ingestion(&state, crate::ingestion::new_shared_status()).await`.
   - Return `Json(json!({"message": "Database refresh started"}))` immediately (don't await ingestion).

2. **Wire watcher restart on agent settings change:** In the `update_agent` handler, after the DB update and broadcast_event call, add watcher restart logic:
   - Lock `state.watcher` mutex.
   - Drop the old watcher handle if present (this sends the shutdown signal via oneshot channel drop).
   - Call `crate::watcher::start_watcher(state.clone(), ...)` with the new settings values from the request body.
   - Store the new handle.
   - Spawn `crate::ingestion::run_ingestion()` to trigger a full re-ingestion with the new paths.

3. **Update test-sync stub message:** Change the 501 message from "Phase 40" to "Remote sync out of scope for v3.0" for clarity. Same for sync-now.

4. **SettingsPage.vue -- Port field with toast:** Add a new section to the Settings page for server port configuration. Place it AFTER the "Agent Configuration" section and BEFORE the "Data Management" section:
   - Add a new card section titled "Server Configuration" with a `Server` icon (import from lucide-vue-next, or reuse `Settings` icon).
   - Add a number input field bound to `form.serverPort` (type="number", min=1024, max=65535, placeholder="8123").
   - Label: "Server Port" with helper text: "Port for the internal API server (default: 8123)".
   - Add a "Save Port" button that:
     - PUTs to `/api/settings/port` with `{ serverPort: form.serverPort }`.
     - On success, shows a toast notification: "Port changed. Restart app to apply." using the existing toast/notification pattern in the codebase. If no toast system exists, use a DaisyUI `alert` that appears temporarily (3s auto-dismiss) below the save button.
   - Load the current `serverPort` value from the GET /api/settings response into `form.serverPort` on page mount (the response already includes it after Task 1 changes).
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/src-tauri && cargo build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - refresh-db endpoint triggers ingestion (no longer 501)
    - Agent settings change restarts file watcher with new paths/toggles
    - test-sync and sync-now remain 501 with updated messages
    - Settings page has port field with number input
    - Saving port shows toast: "Port changed. Restart app to apply."
    - `cargo build` succeeds
  </done>
</task>

</tasks>

<verification>
1. `cd src-tauri && cargo build` succeeds
2. `cargo test watcher` -- all unit tests pass for path classification and debounce
3. `cargo tauri dev` starts the app on port 8123
4. `curl http://127.0.0.1:8123/api/health` returns OK
5. `curl http://127.0.0.1:8123/api/settings` response includes `serverPort: 8123`
6. `curl -X POST http://127.0.0.1:8123/api/settings/refresh-db` returns 200 with "Database refresh started"
7. Settings page shows port configuration field
8. Check terminal output for watcher initialization messages on startup
</verification>

<success_criteria>
- notify-based file watcher starts on app boot watching configured paths
- Debounced events (1s Claude, 3s Cursor) trigger ingestion automatically
- Server runs on configurable port (default 8123) with CSP and Vite proxy updated
- Port configurable via Settings page with toast notification on change
- Settings agent changes restart watcher
- refresh-db triggers full ingestion
- `cargo build` and `cargo test watcher` clean
</success_criteria>

<output>
After completion, create `.planning/phases/40-file-watcher-desktop-chrome/40-01-SUMMARY.md`
</output>
