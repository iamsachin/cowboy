---
phase: 19-conversation-display-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/frontend/src/composables/useGroupedTurns.ts
  - packages/frontend/src/utils/content-sanitizer.ts
  - packages/frontend/tests/utils/content-sanitizer.test.ts
  - packages/frontend/tests/composables/useGroupedTurns.test.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-05
  - CONV-06
  - CONV-08

must_haves:
  truths:
    - "System messages between assistant turns do not break turn grouping into separate groups"
    - "Null or empty content messages are NOT falsely classified as system-injected"
    - "Skill prompt messages containing 'Base directory for this skill:' are detected as system-injected"
    - "System messages between assistant turns appear as a SystemGroup AFTER the assistant group, not between individual turns"
  artifacts:
    - path: "packages/frontend/tests/utils/content-sanitizer.test.ts"
      provides: "Unit tests for isSystemInjected edge cases"
    - path: "packages/frontend/src/utils/content-sanitizer.ts"
      provides: "Fixed isSystemInjected function"
      contains: "if (!content) return false"
    - path: "packages/frontend/src/composables/useGroupedTurns.ts"
      provides: "Fixed groupTurns that does not flush assistant on system messages"
  key_links:
    - from: "packages/frontend/src/composables/useGroupedTurns.ts"
      to: "packages/frontend/src/utils/content-sanitizer.ts"
      via: "isSystemInjected import"
      pattern: "isSystemInjected"
---

<objective>
Fix turn grouping logic and system message detection to prevent assistant groups from being incorrectly split, prevent null/empty content from being falsely hidden, and detect skill prompt patterns.

Purpose: CONV-01 is the highest-severity bug -- system messages between assistant turns break grouping, causing fragmented display. CONV-08 causes real messages to disappear. CONV-05 leaves skill prompts visible as noise. CONV-06 is a natural consequence of CONV-01 fix.
Output: Corrected groupTurns() function, fixed isSystemInjected(), and comprehensive test coverage for both.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-conversation-display-fixes/19-RESEARCH.md

@packages/frontend/src/composables/useGroupedTurns.ts
@packages/frontend/src/utils/content-sanitizer.ts
@packages/frontend/tests/composables/useGroupedTurns.test.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From packages/frontend/src/composables/useGroupedTurns.ts:
```typescript
export interface AssistantGroup {
  type: 'assistant-group';
  turns: AssistantTurn[];
  model: string | null;
  messageCount: number;
  toolCallCount: number;
  firstTimestamp: string;
  lastTimestamp: string;
}

export interface SystemGroup {
  type: 'system-group';
  messages: MessageRow[];
  categories: SystemMessageCategory[];
  count: number;
}

export function groupTurns(messages: MessageRow[], toolCalls: ToolCallRow[]): GroupedTurn[];
```

From packages/frontend/src/utils/content-sanitizer.ts:
```typescript
export function isSystemInjected(content: string | null): boolean;
export function stripXmlTags(text: string): string;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix isSystemInjected and add content-sanitizer tests</name>
  <files>packages/frontend/src/utils/content-sanitizer.ts, packages/frontend/tests/utils/content-sanitizer.test.ts</files>
  <behavior>
    - Test: isSystemInjected(null) returns false (CONV-08)
    - Test: isSystemInjected("") returns false (CONV-08)
    - Test: isSystemInjected("  ") returns false (CONV-08)
    - Test: isSystemInjected("Base directory for this skill: /path/to/skill\nMore instructions...") returns true (CONV-05)
    - Test: isSystemInjected("Hello, how are you?") returns false (normal user message)
    - Test: isSystemInjected with system-reminder XML returns true (existing behavior preserved)
    - Test: isSystemInjected with slash command content returns false (existing behavior preserved)
  </behavior>
  <action>
  1. Create `packages/frontend/tests/utils/content-sanitizer.test.ts` with tests for CONV-05 and CONV-08 behaviors listed above plus existing behavior preservation tests.

  2. Run tests -- they should FAIL for CONV-05 (missing pattern) and CONV-08 (null returns true currently).

  3. Fix `isSystemInjected()` in `content-sanitizer.ts`:
     - Line 64: Change `if (!content) return true;` to `if (!content) return false;`
     - Line 66: Change `if (!trimmed) return true;` to `if (!trimmed) return false;`
     - Add new pattern check before the final `return false`: `if (/^Base directory for this skill:/.test(trimmed)) return true;`

  4. Run tests -- all should PASS.
  </action>
  <verify>
    <automated>cd packages/frontend && npx vitest run tests/utils/content-sanitizer.test.ts</automated>
  </verify>
  <done>isSystemInjected(null) and isSystemInjected("") return false. "Base directory for this skill:" prefix is detected. All existing behavior preserved. Test file created and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix groupTurns system-message-breaks-assistant-group bug and add tests</name>
  <files>packages/frontend/src/composables/useGroupedTurns.ts, packages/frontend/tests/composables/useGroupedTurns.test.ts</files>
  <behavior>
    - Test: Sequence [assistant, system-injected-user, assistant, user] produces [assistant-group(2 turns), system-group, user] -- NOT [assistant-group(1), system-group, assistant-group(1), user]
    - Test: Sequence [assistant, system-injected-user, system-injected-user, assistant, user] produces [assistant-group(2 turns), system-group(2 msgs), user]
    - Test: Sequence [user, assistant, user] still works correctly (no regression)
    - Test: System messages NOT between assistant turns still produce separate SystemGroup (e.g., [system-user, assistant, user] -> [system-group, assistant-group, user])
  </behavior>
  <action>
  1. Add new test cases to the existing `tests/composables/useGroupedTurns.test.ts` file in a new describe block called "CONV-01: system messages between assistant turns".

  2. Run tests -- the CONV-01 test should FAIL (system message currently breaks assistant group).

  3. Fix `groupTurns()` in `useGroupedTurns.ts` at line 234-237. Remove the `flushAssistant()` call inside the `isSystemInjected` branch:

  ```typescript
  // BEFORE (line 234-237):
  if (isSystemInjected(content)) {
    flushAssistant();
    pendingSystem.push(msg);
    continue;
  }
  ```

  Change to:

  ```typescript
  // AFTER:
  if (isSystemInjected(content)) {
    pendingSystem.push(msg);
    continue;
  }
  ```

  This means system messages accumulate in `pendingSystem` without flushing the current assistant group. When the assistant group eventually flushes (on a real user message, slash command, clear, or end-of-list), `flushSystem()` is called right after, so system messages appear AFTER the assistant group (CONV-06 satisfied).

  4. Run tests -- all should PASS including new CONV-01 tests.

  5. Run the full frontend test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd packages/frontend && npx vitest run</automated>
  </verify>
  <done>System-injected messages between assistant turns no longer break grouping. The sequence [assistant, system, assistant] produces a single assistant-group with 2 turns, followed by a system-group. All existing groupTurns tests still pass.</done>
</task>

</tasks>

<verification>
- `cd packages/frontend && npx vitest run` -- all tests green
- content-sanitizer.test.ts covers CONV-05 and CONV-08 edge cases
- useGroupedTurns.test.ts covers CONV-01 grouping fix and CONV-06 positioning
</verification>

<success_criteria>
- isSystemInjected(null) returns false, not true
- isSystemInjected("Base directory for this skill:...") returns true
- groupTurns does not flush assistant group when system-injected message appears
- System messages appear AFTER assistant groups, not between individual assistant turns
- All tests pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/19-conversation-display-fixes/19-01-SUMMARY.md`
</output>
