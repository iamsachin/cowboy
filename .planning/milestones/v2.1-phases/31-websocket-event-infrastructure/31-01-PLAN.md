---
phase: 31-websocket-event-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/types/websocket-events.ts
  - packages/shared/src/types/index.ts
  - packages/backend/src/plugins/websocket.ts
  - packages/backend/src/ingestion/index.ts
  - packages/backend/src/app.ts
  - packages/backend/src/routes/settings.ts
  - packages/backend/tests/websocket.test.ts
autonomous: true
requirements:
  - PUSH-02

must_haves:
  truths:
    - "Backend emits conversation:changed with conversationId and changes array when a conversation is updated during ingestion"
    - "Backend emits conversation:created with summary data when a new conversation is first ingested"
    - "Backend emits system:full-refresh when settings clear-db or refresh-db is triggered"
    - "All events carry monotonic sequence numbers"
  artifacts:
    - path: "packages/shared/src/types/websocket-events.ts"
      provides: "Discriminated union event types, ChangeType, type guards"
      exports: ["WebSocketEvent", "WebSocketEventType", "ConversationChangedEvent", "ConversationCreatedEvent", "SystemFullRefreshEvent", "ChangeType", "isConversationChanged", "isConversationCreated", "isSystemFullRefresh"]
    - path: "packages/backend/src/plugins/websocket.ts"
      provides: "broadcastEvent decorator with sequence counter"
      contains: "broadcastEvent"
    - path: "packages/backend/src/ingestion/index.ts"
      provides: "Per-conversation diff tracking during ingestion"
      contains: "ChangeType"
  key_links:
    - from: "packages/backend/src/ingestion/index.ts"
      to: "packages/backend/src/app.ts"
      via: "onIngestionComplete callback receives change events"
      pattern: "onIngestionComplete.*WebSocketEvent"
    - from: "packages/backend/src/app.ts"
      to: "packages/backend/src/plugins/websocket.ts"
      via: "app.broadcastEvent() calls"
      pattern: "broadcastEvent"
    - from: "packages/shared/src/types/websocket-events.ts"
      to: "packages/backend/src/plugins/websocket.ts"
      via: "shared type imports"
      pattern: "import.*websocket-events"
---

<objective>
Define shared WebSocket event types and implement typed event emission on the backend. After this plan, the backend broadcasts conversation:changed, conversation:created, and system:full-refresh events with sequence numbers instead of the untyped data-changed broadcast.

Purpose: Foundation for typed event routing — frontend plan (31-02) depends on these event shapes.
Output: Shared type definitions, backend broadcastEvent decorator, ingestion diff tracking, settings event migration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-websocket-event-infrastructure/31-CONTEXT.md
@.planning/phases/31-websocket-event-infrastructure/31-RESEARCH.md

@packages/shared/src/types/index.ts
@packages/backend/src/plugins/websocket.ts
@packages/backend/src/ingestion/index.ts
@packages/backend/src/app.ts
@packages/backend/src/routes/settings.ts

<interfaces>
<!-- Current backend broadcast decorator (to be replaced) -->
From packages/backend/src/plugins/websocket.ts:
```typescript
declare module 'fastify' {
  interface FastifyInstance {
    broadcast: (message: object) => void;
  }
}
```

From packages/backend/src/ingestion/index.ts:
```typescript
export interface IngestionPluginOptions {
  basePath?: string;
  autoIngest?: boolean;
  onIngestionComplete?: () => void;
}
```

From packages/backend/src/app.ts (line 39-41):
```typescript
onIngestionComplete: () => {
  app.broadcast({ type: 'data-changed', timestamp: new Date().toISOString() });
},
```

Settings broadcast points (packages/backend/src/routes/settings.ts lines 194, 210):
```typescript
app.broadcast({ type: 'data-changed', timestamp: new Date().toISOString() });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared event types and backend broadcastEvent decorator</name>
  <files>packages/shared/src/types/websocket-events.ts, packages/shared/src/types/index.ts, packages/backend/src/plugins/websocket.ts</files>
  <action>
1. Create `packages/shared/src/types/websocket-events.ts` with:
   - `ChangeType` union: `'messages-added' | 'tool-calls-added' | 'tokens-updated' | 'plan-updated' | 'status-changed' | 'metadata-changed'`
   - `ConversationChangedEvent` interface: `{ type: 'conversation:changed', seq: number, conversationId: string, changes: ChangeType[], timestamp: string }`
   - `ConversationCreatedEvent` interface: `{ type: 'conversation:created', seq: number, conversationId: string, summary: { title: string | null, agent: string, project: string | null, createdAt: string }, timestamp: string }`
   - `SystemFullRefreshEvent` interface: `{ type: 'system:full-refresh', seq: number, timestamp: string }`
   - `WebSocketEvent` discriminated union of all three
   - `WebSocketEventType = WebSocketEvent['type']`
   - Type guard functions: `isConversationChanged()`, `isConversationCreated()`, `isSystemFullRefresh()`
   - Export everything

2. Update `packages/shared/src/types/index.ts` to re-export all from `./websocket-events.js`

3. Refactor `packages/backend/src/plugins/websocket.ts`:
   - Import `WebSocketEvent` from `@cowboy/shared`
   - Add module-level `let seq = 0` counter
   - Replace `broadcast` decorator with `broadcastEvent` decorator that accepts `Omit<WebSocketEvent, 'seq'>`, injects `seq: ++seq`, serializes and sends to all open clients
   - Keep the old `broadcast` decorator temporarily as an alias that calls `broadcastEvent` (settings routes still use it until Task 2 migrates them) — actually NO, migrate everything in Task 2 so just replace `broadcast` with `broadcastEvent` and update the Fastify type declaration accordingly
   - Update the Fastify module declaration: `broadcastEvent: (event: Omit<WebSocketEvent, 'seq'>) => void`
   - Keep the `connected` message on socket open (it's not a WebSocketEvent, just a handshake signal)
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/packages/shared && npx tsc --noEmit && cd ../backend && npx tsc --noEmit</automated>
  </verify>
  <done>Shared types compile, backend compiles with new broadcastEvent decorator, sequence numbers injected on every event</done>
</task>

<task type="auto">
  <name>Task 2: Ingestion diff tracking and event emission migration</name>
  <files>packages/backend/src/ingestion/index.ts, packages/backend/src/app.ts, packages/backend/src/routes/settings.ts, packages/backend/tests/websocket.test.ts</files>
  <action>
1. Modify `packages/backend/src/ingestion/index.ts`:
   - Import `WebSocketEvent`, `ChangeType`, `ConversationChangedEvent`, `ConversationCreatedEvent` from `@cowboy/shared`
   - Change `IngestionPluginOptions.onIngestionComplete` signature to `(events: Omit<WebSocketEvent, 'seq'>[]) => void` (the caller receives events without seq; broadcastEvent adds seq)
   - Inside `runIngestion()`, create a `const collectedEvents: Omit<WebSocketEvent, 'seq'>[] = []` array before the file loop
   - For each Claude Code conversation file inside the `for (const file of files)` loop, BEFORE the transaction:
     - Query existing conversation: `const existingConv = db.select({ id: conversations.id }).from(conversations).where(eq(conversations.id, normalizedData.conversation.id)).get()`
     - Query existing message count: `const existingMsgCount = existingConv ? db.select({ count: sql<number>'count(*)' }).from(messages).where(eq(messages.conversationId, normalizedData.conversation.id)).all()[0].count : 0`
     - Similarly capture existing tool call count, token usage count, plan count, and conversation status/title/model from the conversations row
   - AFTER the transaction, compare:
     - If `!existingConv` (new conversation), push a `conversation:created` event with summary (title, agent, project, createdAt from normalizedData.conversation)
     - If existing, build a `changes: ChangeType[]` array: compare new vs old message count (messages-added), tool call count (tool-calls-added), token count (tokens-updated), plan count (plan-updated), status change (status-changed), title/model change (metadata-changed). If changes.length > 0, push `conversation:changed` event
   - Apply the SAME diff logic to the Cursor ingestion loop (the second `for` loop processing Cursor conversations)
   - Consider extracting a helper function `trackChanges(conversationId, normalizedData, tx)` to avoid duplication between Claude Code and Cursor loops
   - In the `finally` block, call `opts.onIngestionComplete?.(collectedEvents)` instead of `opts.onIngestionComplete?.()`

2. Update `packages/backend/src/app.ts`:
   - Change `onIngestionComplete` callback to accept events parameter: `onIngestionComplete: (events) => { for (const event of events) { app.broadcastEvent(event); } }`
   - Remove the old `app.broadcast({ type: 'data-changed' ... })` call

3. Update `packages/backend/src/routes/settings.ts`:
   - Replace both `app.broadcast({ type: 'data-changed' ... })` calls (lines ~194 and ~210) with `app.broadcastEvent({ type: 'system:full-refresh', timestamp: new Date().toISOString() })`

4. Update `packages/backend/tests/websocket.test.ts`:
   - Replace all assertions on `data-changed` event type with assertions on the new typed events
   - Test that a typed `conversation:changed` event includes `seq`, `conversationId`, `changes`, `timestamp`
   - Test that `system:full-refresh` events are emitted for settings operations
   - Test that sequence numbers are monotonically increasing across multiple events

IMPORTANT: The ingestion diff must happen OUTSIDE the transaction (after commit), not inside, to ensure data is committed before events are emitted. Collect changes during the loop, emit all at the end via onIngestionComplete in the `finally` block.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy/packages/backend && npx vitest run tests/websocket.test.ts -x && npx tsc --noEmit</automated>
  </verify>
  <done>Ingestion emits typed conversation:changed and conversation:created events with correct change arrays. Settings routes emit system:full-refresh. Backend tests pass asserting on typed event shapes and sequence numbers. No references to data-changed remain in backend code.</done>
</task>

</tasks>

<verification>
- `cd packages/shared && npx tsc --noEmit` -- shared types compile
- `cd packages/backend && npx tsc --noEmit` -- backend compiles with new event system
- `cd packages/backend && npx vitest run tests/websocket.test.ts -x` -- backend WebSocket tests pass
- `grep -r "data-changed" packages/backend/src/` returns no results (fully migrated)
- `grep -r "broadcastEvent" packages/backend/src/` shows usage in websocket.ts, app.ts, settings.ts
</verification>

<success_criteria>
Backend broadcasts typed WebSocket events with conversation IDs, change arrays, and monotonic sequence numbers. Shared type package exports all event interfaces and type guards. No untyped data-changed events remain in backend. Backend tests validate new event shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/31-websocket-event-infrastructure/31-01-SUMMARY.md`
</output>
