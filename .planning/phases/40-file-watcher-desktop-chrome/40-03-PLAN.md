---
phase: 40-file-watcher-desktop-chrome
plan: 03
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - packages/frontend/src/types/analytics.ts
  - packages/frontend/src/types/api.ts
  - packages/frontend/src/types/database.ts
  - packages/frontend/src/types/pricing.ts
  - packages/frontend/src/types/websocket-events.ts
  - packages/frontend/src/types/index.ts
  - packages/frontend/package.json
  - package.json
  - pnpm-workspace.yaml
autonomous: true
requirements: [DESK-03]

must_haves:
  truths:
    - "packages/backend directory no longer exists"
    - "packages/shared directory no longer exists"
    - "All 37 frontend files that imported from @cowboy/shared now import from local types"
    - "pnpm --filter @cowboy/frontend build succeeds"
    - "cargo tauri dev is the only way to run the app"
    - "No references to @cowboy/backend or @cowboy/shared remain in any config file"
    - "scripts/diff-backends.sh and scripts/diff-ingestion.sh no longer exist"
  artifacts:
    - path: "packages/frontend/src/types/index.ts"
      provides: "Consolidated type barrel re-exporting all shared types"
    - path: "package.json"
      provides: "Root package.json with only tauri-related scripts"
      contains: "tauri"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace config with only frontend package"
  key_links:
    - from: "packages/frontend/src/**"
      to: "packages/frontend/src/types/"
      via: "local imports replacing @cowboy/shared"
      pattern: "from.*['\"].*types"
---

<objective>
Remove the Node.js backend, merge shared types into frontend, and clean up all configuration so `cargo tauri dev` is the sole entry point.

Purpose: Complete the Tauri migration by eliminating the Node.js backend entirely. The Rust backend (from Phases 36-39) replaces it completely.
Output: Clean monorepo with only packages/frontend, Tauri as the backend, no dead packages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-file-watcher-desktop-chrome/40-CONTEXT.md
@.planning/phases/40-file-watcher-desktop-chrome/40-01-SUMMARY.md

@packages/frontend/vite.config.ts
@packages/frontend/package.json
@package.json
@pnpm-workspace.yaml

<interfaces>
<!-- Shared types that must be migrated -->

From packages/shared/src/types/index.ts (barrel export):
```typescript
// Type-only exports
export type { Conversation, Message, ToolCall, TokenUsage } from './database.js';
export type { HealthResponse, OverviewStats, TimeSeriesPoint, ... } from './api.js';
export type { ModelPricing } from './pricing.js';
export type { Granularity } from './analytics.js';
export type { ChangeType, ConversationChangedEvent, ... } from './websocket-events.js';

// Value exports (functions/constants)
export { MODEL_PRICING, calculateCost } from './pricing.js';
export { autoGranularity } from './analytics.js';
export { isConversationChanged, isConversationCreated, isSystemFullRefresh, isSettingsChanged } from './websocket-events.js';
```

Files: analytics.ts (14 lines), api.ts (256 lines), database.ts (42 lines), pricing.ts (76 lines), websocket-events.ts (77 lines)

37 frontend files import from @cowboy/shared (see research).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate shared types to frontend and update all imports</name>
  <files>packages/frontend/src/types/analytics.ts, packages/frontend/src/types/api.ts, packages/frontend/src/types/database.ts, packages/frontend/src/types/pricing.ts, packages/frontend/src/types/websocket-events.ts, packages/frontend/src/types/index.ts</files>
  <action>
1. **Copy shared type files to frontend:** Copy all 5 type files from `packages/shared/src/types/` to `packages/frontend/src/types/`:
   - `analytics.ts`, `api.ts`, `database.ts`, `pricing.ts`, `websocket-events.ts`
   - If any of these filenames already exist in `packages/frontend/src/types/`, merge the content (shared types take priority since they're the canonical definitions).

2. **Create/update barrel index:** Create `packages/frontend/src/types/index.ts` that re-exports everything from the 5 type files, matching the exact same exports as the current `packages/shared/src/types/index.ts`. Change `.js` extensions to `.ts` in the re-exports (or remove extensions entirely -- Vite resolves both).

3. **Update all 37 frontend imports:** Replace every `from '@cowboy/shared'` import with the correct relative path to `packages/frontend/src/types/` (or `packages/frontend/src/types/index`). The relative path depends on the importing file's depth:
   - Files in `src/composables/`: `from '../types'`
   - Files in `src/components/`: `from '../types'`
   - Files in `src/pages/`: `from '../types'`
   - Files in `tests/composables/`: `from '../../src/types'`
   - Files in `tests/utils/`: `from '../../src/types'`

   Be precise: some files may import types only (`import type { ... }`), others import values (`import { autoGranularity }`). Both must work.

4. **Remove @cowboy/shared from frontend package.json:** In `packages/frontend/package.json`, remove the `@cowboy/shared` dependency from `dependencies` (it's likely a workspace reference like `"@cowboy/shared": "workspace:*"`).

5. **Verify build:** Run `pnpm --filter @cowboy/frontend build` and ensure zero TypeScript errors. All type re-exports must resolve.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy && pnpm --filter @cowboy/frontend build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All 37 files import from local `../types` (or equivalent relative path)
    - No references to `@cowboy/shared` remain in frontend source
    - `pnpm --filter @cowboy/frontend build` succeeds with zero errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete Node.js backend, shared package, scripts, and clean configs</name>
  <files>package.json, pnpm-workspace.yaml</files>
  <action>
1. **Delete packages/backend:** `rm -rf packages/backend` -- complete removal, no archive.

2. **Delete packages/shared:** `rm -rf packages/shared` -- already migrated to frontend.

3. **Delete migration scripts:** `rm -f scripts/diff-backends.sh scripts/diff-ingestion.sh`. If `scripts/` is now empty, `rm -rf scripts/`.

4. **Update pnpm-workspace.yaml:** Keep the workspace format but it should only reference frontend:
   ```yaml
   packages:
     - 'packages/*'
   ```
   This is fine as-is since only `packages/frontend` remains. Alternatively simplify to `- 'packages/frontend'` for clarity.

5. **Update root package.json:**
   - Remove the `"dev"` script (which runs concurrently backend+frontend). `cargo tauri dev` replaces it.
   - Remove the `"build"` script (or change to `pnpm --filter @cowboy/frontend build` only -- Tauri's `beforeBuildCommand` in tauri.conf.json already handles this).
   - Remove `"db:generate"` and `"db:migrate"` scripts (Drizzle is gone with the Node.js backend).
   - Remove `concurrently` from devDependencies (no longer needed).
   - Remove `"pnpm": { "onlyBuiltDependencies": ["better-sqlite3", ...] }` since better-sqlite3 was the Node.js SQLite driver.
   - Keep `@tauri-apps/cli`, `typescript`, and `vitest` in devDependencies.
   - Add a simple `"dev": "cargo tauri dev"` script for convenience (optional but helpful).

6. **Run pnpm install:** After removing packages and updating workspace, run `pnpm install` to clean up the lockfile.

7. **Verify full build:** Run `cargo tauri build --debug` or at minimum `cd src-tauri && cargo build` to ensure everything still compiles.
  </action>
  <verify>
    <automated>cd /Users/sachin/Desktop/learn/cowboy && test ! -d packages/backend && test ! -d packages/shared && test ! -f scripts/diff-backends.sh && pnpm --filter @cowboy/frontend build 2>&1 | tail -5 && echo "CLEANUP VERIFIED"</automated>
  </verify>
  <done>
    - packages/backend directory deleted
    - packages/shared directory deleted
    - scripts/diff-backends.sh and diff-ingestion.sh deleted
    - Root package.json has no @cowboy/backend or concurrently references
    - pnpm-workspace.yaml is clean
    - `pnpm --filter @cowboy/frontend build` succeeds
    - `cargo tauri dev` is the only way to run the app
  </done>
</task>

</tasks>

<verification>
1. `test ! -d packages/backend && test ! -d packages/shared` -- packages gone
2. `grep -r "@cowboy/shared" packages/frontend/src/ --include="*.ts" --include="*.vue"` -- returns nothing
3. `pnpm --filter @cowboy/frontend build` succeeds
4. `cd src-tauri && cargo build` succeeds
5. `cargo tauri dev` starts the app successfully
</verification>

<success_criteria>
- No Node.js backend code remains
- No shared package remains (types migrated to frontend)
- All 37 frontend files use local type imports
- Frontend builds successfully
- Tauri app builds and runs
- `cargo tauri dev` is the sole entry point
</success_criteria>

<output>
After completion, create `.planning/phases/40-file-watcher-desktop-chrome/40-03-SUMMARY.md`
</output>
